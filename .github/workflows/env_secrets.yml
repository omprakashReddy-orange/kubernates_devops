name: env_secrets.yml
on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: install
        env:
          SECRET: dev
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run:
          echo "environment variable SECRET is ${SECRET}"
          echo "environment variable DB_PASSWORD is ${DB_PASSWORD}"

